# Inspired by https://community.element14.com/technologies/code_exchange/b/blog/posts/github-automate-project-documentation-with-doxygen-and-publish-online-after-pushing-commits
name: Build, Check, and Deploy doxygen docs to gh pages

on:
    # Runs on pushes targeting the default branch
    push:
        branches: ["doxygen"]

    # Allows you to run this workflow manually from the Actions tab
    workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
    contents: read
    pages: write
    id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
    group: "pages"
    cancel-in-progress: false

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            # - name: Setup Pages
            #   uses: actions/configure-pages@v5
            - name: Run Doxygen
              uses: mattnotmitt/doxygen-action@v1.9.5
              with:
                  working-directory: "."
                  doxyfile-path: "Doxyfile"
            - name: Upload pages artifact
              uses: actions/upload-artifact@v5
              with:
                  path: html
                  name: grid
            # - name: Upload error log
            #   uses: actions/upload-artifact@v4
            #   with:
            #     path: Demo/doxygen_warnings.txt
            #     name: doxygen_warnings

    # check:
    #     runs-on: ubuntu-latest
    #     needs: build
    #     steps:
    #         - name: Fetch artifact
    #           uses: actions/download-artifact@v5
    #           with:
    #             name: doxygen_warnings
    #         - name: Check artifact
    #           run: |
    #             if [ -s doxygen_warnings.txt ]; then
    #               echo "Warnings in build:"
    #               cat doxygen_warnings.txt
    #               exit 1
    #             else
    #               echo "All good!"
    #             fi

    # deploy:
    #     environment:
    #         name: github-pages
    #         url: ${{ steps.deployment.outputs.page_url }}
    #     runs-on: ubuntu-latest
    #     needs: build
    #     steps:
    #         - name: Deploy to GitHub Pages
    #           id: deployment
    #           uses: actions/deploy-pages@v4
